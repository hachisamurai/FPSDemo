# FPSDemo 开发规则

## 项目目标
- UE 5.4 C++ 第一人称 GAS 十关闯关 Demo；大厅选择三个检查点存档栏之一，新档进入安全区并在下一关终端选择简单/普通/困难。前九关原地清关并提供能力选择，在本关中心生成升级与下一关终端，第 5/10 关包含 Boss。Esc暂停并提供安全区/设置/大厅/退出；未通关主动返回安全区清空银币/本局成长并保留难度，已有成长先确认，永久武器解锁独立保留。玩家死亡结算后回安全区重开第一关；复用三个白模战斗区，切换战斗清理旧终端。怪物等级对应 1..10 关，属性先乘等级倍率再乘难度倍率，击杀银币按等级配置，整轮通关金币按难度配置。默认支持单人 Standalone / 单人 PIE。
- 双币规则：击杀给银币，完成十关整轮挑战才按DT_Difficulties.VictoryGoldReward发金币（简单500/普通750/困难1000默认）。安全区金币升级永久保留；银币升级与免费关卡奖励属于临时成长。两币种×三属性各自20起价、所购项+10，其他项不涨价。通关/死亡/主动放弃只清空银币及临时成长/银币价格，保留金币余额/永久属性/金币价格/武器解锁。检查点V4记录V3来源账本及本槽弹药解锁/装配（normal/fire/frost/piercing）；安全区金币购买弹药，独立定价，购买/装配立即保存，死亡/通关保留，旧V1/V2退还可推算金币花费，旧合计属性暂留至本轮结束；Editor停止试玩仍清空全部临时存档。
- Editor构建仅使用每次GI独立的本地临时存档/解锁，停止PIE或Editor独立试玩时清理；同一次运行内跨地图/返回大厅保留，非Editor游戏继续持久化。Editor不创建云同步子系统。设备设置、日志和截图不属于试玩进度。
- 参考 `F:/Lyra/Lyra` 的 ASC 所有权、Avatar 初始化、属性结算思路，不引入 Lyra 插件依赖。

- 冲刺/治疗允许Combat、清关Intermission和Hub；菜单/暂停/奖励/结算/死亡/大厅阻塞。武器仍限Combat。HUD与GA共用CanUsePlayerSkills，普通阶段切换不刷新冷却。

## 强制日志与注释
- 每个新增或修改的功能函数、接口、委托回调、定时器回调、生命周期函数都必须在入口记录调用日志；拒绝分支、状态切换和失败原因也应记录。
- 使用 `DemoLog.h` 的 `DEMO_LOG_CALL()`；高频函数使用 `DEMO_LOG_TICK()`。Editor默认开启全部调用日志；非Editor包体的LogFPSDemo/LogDemoOnline编译上限为Display，仅保留关键事件、警告和错误。禁止省略源码埋点；用类别编译上限降噪，不能将调用/逐帧日志提升为Display。Shipping目标使用独立构建环境启用关键日志。
- `Demo.Debug.UnlockAll`仅在WITH_EDITOR注册，GI持有本次试玩全枪械/弹药权限；停止清空，不伪造通关/金币/云记录。命令及调试接口不得进入任何非Editor包体。
- 新增或修改的变量、成员、参数、配置项必须在声明附近说明用途，按需补充单位、范围、所有权、生命周期和复制语义。
- 每个函数在声明附近解释功能、调用时机、输入输出、关键限制；实现附近解释设计意图和边界。修改逻辑时同步更新旧注释。
- Lambda 必须说明捕获、目的、触发时机、生命周期；异步/跨线程还需说明对象有效性和线程边界。优先 UObject 绑定委托，避免悬空捕获。
- 不支持注释的格式，在对应文档中记录字段含义与修改原因。引擎/UHT 自动生成代码不手工编辑。

## 模块说明文档

- 存档运行环境：WITH_EDITOR目标（含PIE、编辑器Standalone和Editor-Cmd -game）只使用本地数据，不创建HTTP或云同步子系统；仅打包游戏使用独立API访问MongoDB。不要增加可绕过此限制的编辑器联网测试开关，云回归使用隔离账号的打包Development进程。
- 新功能或实质性变更必须同步维护 `Documentation/` 中对应模块说明，优先更新已有文档。
- 至少包含：目的、设计理念、核心运行流程、使用配置、依赖接口、边界与失败处理、验证方法、影响文件、维护注意事项。
- 编译/自动化测试结果与尚未验证的项目必须如实区分；不得将网络复制基础等同于完整联机支持。

