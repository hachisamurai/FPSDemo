<Project Sdk="Microsoft.NET.Sdk">
  <!-- 直接编译生产纯规则，验证无需启动HTTP/MongoDB，也不接触玩家账号。 -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="../FPSDemo.Api/WeaponUnlockRules.cs" Link="WeaponUnlockRules.cs" />
  </ItemGroup>
</Project>
