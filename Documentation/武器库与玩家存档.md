# 终端武器库与玩家永久档案

## 目的与规则

终端新增“属性升级 / 武器”页签。武器卡片显示轮廓Icon、名字、解锁状态、每弹丸基础伤害及弹丸数、RPM、基础弹匣容量、装填秒数。未解锁卡片为灰色，但仍能点击查看Tips。选中卡片只打开说明，明确点击“装备为主武器”才会生成或切换主武器。

| 稳定ID | 武器 | 永久解锁条件 | 槽位 |
|---|---|---|---|
| `pistol` | 手枪 | 新档默认解锁 | 固定副武器，按2 |
| `rifle` | 步枪 | 完整通关简单难度十关 | 主武器，按1 |
| `shotgun` | 散弹枪 | 完整通关普通难度十关 | 主武器，按1 |
| `sniper` | 狙击枪 | 完整通关困难难度十关 | 主武器，按1 |

三档条件独立，困难通关不替代普通/简单记录。新建存档、主动放弃或死亡重载后只生成手枪，即使所有主武器已解锁，也需再次前往终端装备。通关返回安全区保留解锁和当前库存，清空临时属性成长/银币，保留金币永久成长并按基础容量加永久容量补给，新增解锁不直接发枪，玩家可立即前往终端装备。已有检查点恢复其真实库存，与偏好自动发枪不同。装备不收费；金币用于安全区属性升级，银币用于关间属性升级，两者不用于装备枪械。解锁与最近选择写入本地档案，但最近选择只是偏好记录。

## 设计理念与职责

- `UDemoPlayerProfile` 是自动创建的 GameInstanceSubsystem，拥有跨地图的纯数据档案、读写和上传边界；无需换项目GameInstance类。
- `DemoWeaponCatalog` 用稳定ID连接规则/存档/UI，不把翻译文案、数组顺序或蓝图路径写进协议。当前目录0手枪/1步枪/2散弹/3狙击，组件主型号索引仍为0/1/2。
- `UDemoWeaponComponent` 管当前Pawn库存与装备。初始化缓存四个蓝图类CDO供UI读取真实配置，仅Spawn手枪。主枪首次在终端装备时才Spawn；本局曾持有的主枪保留隐藏实例，切回不会刷新弹药。
- `ADemoPlayerController` 管理页签、Tips及模态输入，HUD只读数据/派发意图；`AFPSDemoGameMode::GetTerminalBlockReason` 共用权威条件，组件内部再次检查。UI禁用不能代替权限检查。
- `ADemoHUD` 沿用Canvas与1280×720设计坐标。Icon优先读取武器蓝图Config中的Texture2D软引用，异步加载后缓存；未配置/加载中/失败时显示四种原生枪械轮廓。卡片属性来自已有蓝图Config/CDO，未硬编码第二套平衡数值。

## 核心执行流程

1. 非Editor游戏的GI启动加载`DemoPlayerProfile`槽；Editor每次试玩创建独立临时槽。新档生成GUID、默认手枪解锁；有效旧档校验版本、GUID、修订、通关ID与时间，从通关凭据重新推导解锁列表。篡改列表本身不会成为解锁来源。
2. Pawn初始化只装备手枪。安全区或关间备战与当前升级终端交互，打开属性页；点击“武器”切页。
3. 任意卡片可弹出说明。Tips期间不注册底层卡片、页签或保存按钮热区，Tab先关Tips，再按Tab关闭终端。武器页数字键不会购买属性，也不会触发武器槽切换。
4. 装备点击重新验证：单人权威、当前Pawn与Controller、存活、Hub/Intermission、正在武器页、真实当前终端存在、距离≤250cm、ID有效且已解锁。装配失败保留旧装备；成功取消射击/装填/瞄准，设置主槽并保存最近选择。
5. 关闭终端后1/2切换本局已经选择的主武器与手枪；空主槽拒绝。旧B输入保留日志兼容入口但不再轮换主武器，HUD改为终端提示。
6. GameMode成功开始战役时生成RunId；第十关完成并进入Victory时记录对应难度凭据，按RunId去重，推进档案修订并保存。重复回调不重复计入。前九关清场、奖励和失败不授予解锁。胜利页显示解锁武器和本地保存状态。返回Hub继续挑战时，先确认旧RunId再生成新RunId，因此第二轮不同难度可独立解锁；旧Victory存档读取也走同一幂等入口。

## 数据结构

定义位于 `Source/FPSDemo/Player/DemoPlayerProfile.h`。二进制外壳 `UDemoProfileSave` 仅包含 `FDemoPlayerProfileData Data`；局内金币、GAS数值、弹药、Actor/类路径、登录令牌均不写入永久档案。

| 字段 | 类型/默认 | 用途和约束 |
|---|---|---|
| SchemaVersion | int32 / 2 | 档案结构版本；未知未来版本保护原文件，禁止降级覆盖 |
| ProfileId | GUID字符串 | 本地玩家标识；不是服务器账号凭证 |
| LocalRevision | int32 / 首次保存为1 | 每次内容变更递增，JSON安全整数；达到上限保护并要求版本迁移 |
| SyncedRevision | int32 / 0 | 已确认的本地版本，必须0≤它≤LocalRevision；小于本地版本即待同步 |
| ServerRevision | 字符串 / 空 | 服务端不透明并发控制令牌；离线无值 |
| UpdatedUtc | ISO8601字符串 | 内容修改UTC时间，不能用于服务端判定合法性或覆盖顺序 |
| Clears | FDemoClearRecord数组 / 空 | 每项RunId、DifficultyId、CompletedUtc；只记录完整战役，RunId去重 |
| UnlockedWeaponIds | FName数组 / pistol | 稳定目录ID派生缓存；加载按云端确认难度与待上传Clears共同重建 |
| LastSelectedPrimary | FName / None | 最近成功装备的主枪ID；未解锁/手枪/未知ID会被清除，不自动发枪 |

`FDemoClearRecord` 的 `DifficultyId` 只允许 `easy / normal / hard`。V2将Clears作为待上传队列；服务器确认且本地落盘成功后才清理对应记录，完整历史保存在MongoDB的campaign_runs集合。CloudClearedDifficulties保留确认难度基线，RecentAcceptedRuns最多128项防近期重复回调。PreferenceRevision/SyncedPreferenceRevision独立记录武器偏好变更与确认，分批通关确认不会吞掉偏好。V1完整通关记录在加载时迁移为待确认事实。当前一个匿名账号对应一个永久档案，多账号切换UI未实现。

## 本地存档、恢复与失败处理

正式非Editor游戏的标准路径为`Saved/SaveGames/DemoPlayerProfile.sav`，备份`DemoPlayerProfile_Backup.sav`；打包后实际根路径由UE平台SaveGame系统决定，不硬编码工程磁盘路径。平台UserIndex为0。

Editor的PIE/SIE/Standalone每个GI使用`DemoEditorProfile_<PID>_<GUID>`本地临时槽，停止时删除主档和备份、清空解锁/通关；同次运行跨地图和返回大厅保留。强制停止Standalone由父Editor监视器补做清理；不读取正式或云进度。完整流程、边界与实际启停验证见[暂停与检查点存档](暂停设置与检查点存档.md)。

每次内容变更同步保存：先把有效旧主档写入备份，再写新主档。备份失败时不继续覆盖主档，内存仍保留新进度；主档写入失败时同样保留内存和错误提示。下次修改、“保存重试”及正式游戏子系统退出会尝试再次写入，Editor停止则直接清理临时档。此实现有一代备份，不宣称磁盘断电的事务级原子性。

主档损坏或缺失时尝试有效备份；不把损坏主档覆盖到好备份。备份恢复会标记待修复，下一次保存修复主档。主档为未来SchemaVersion时不回退旧备份覆盖它；主备均无效或类型不符时进入只读保护，保留文件，UI明确提示。加载失败的新会话可继续玩，但不能把未保存进度声称已落盘。

`ReloadFromDisk()` 是开发/未来档案管理入口，存在未落盘变更时拒绝，避免老文件覆盖内存新进度。没有新增“清空玩家存档”按钮或自动删除正式存档行为。

## 实际云同步边界

编辑器/PIE/Editor Standalone仅本地存档，不创建HTTP/云同步子系统。以下云流程只在打包游戏启用；编辑器不显示云状态和云操作按钮。

正式联网由UDemoCloudSync调用独立FPSDemoOnline模块，以协议2访问ASP.NET Core后端。永久进度和三个战役检查点都同步，但本地数据仍分开保存。启动认证后优先重放耐久发件箱，再绑定/下载/合并；超时重用同一请求ID，服务器确认并本地落盘后才清理发件箱。

ApplyCloud校验服务器投影，将确认难度和未确认本地记录合并，保留尚未上传的装备偏好。最近主武器始终只是偏好，下载解锁不生成装备。检查点按槽版本检测冲突，大厅提供保留本机/使用云端并先归档两份副本。武器页显示本地保存与云同步状态，“保存重试”同时重试本地落盘与网络连接。

BuildUploadJson/AcknowledgeUpload作为原有离线诊断与回归接口保留，不是生产HTTP提交/确认路径。其protocolVersion=1导出不得发送给新后端；生产同步只能通过请求ID、绑定与账号匹配的协议2回执调用ApplyCloud。

后端接受的单人离线通关标记offline_accepted；认证、幂等、字段校验不等于服务器验证了战斗真实性。实际集合/事务/协议见[MongoDB玩家存档设计](MongoDB玩家存档设计.md)，配置和验证见[云存档运行与验证](云存档运行与验证.md)。
## 使用与配置

- 打开编辑器后从大厅开始，走到升级终端按E，点击“武器”。新档只能查看三把锁定主武器的解锁条件；手枪卡说明固定副槽。
- 修改伤害/射速等在 `/Game/Weapons/Blueprints/BP_Weapon_*` 的Config中进行，武器页自动读取CDO；卡片明确展示基础数值，当前局GAS加成另算。
- 图标在同一武器蓝图的 `Class Defaults → Config → UI → Weapon Icon` 设置，字段为 `FDemoWeaponConfig::Icon`。选入Texture2D即可，不填写Windows文件路径；默认留空保留原轮廓，美术图标不随本次代码修改自动生成。
- 目录目前固定四种；调整目录/组件软类列表必须一起维护ID、图标、难度对应、存档规范化及测试。旧存档升级必须显式迁移Schema，不通过修改显示名称迁移数据。
- 依赖现有GAS/Canvas/EnhancedInput及SaveGame/GI，新增运行时Json、JsonUtilities模块；无Lyra或网络服务依赖。

### 可配置Icon：软引用、异步加载与生命周期

选择 `TSoftObjectPtr<UTexture2D>`，而不是裸FString路径或硬Texture指针：编辑器提供限Texture2D类型的资源选择器，蓝图中保存资产软路径，支持引擎资源依赖追踪；读取配置不要求立刻将纹理载入内存。当前四种武器沿用Config最简单；以后扩展大量武器时可把目录元数据搬到PrimaryDataAsset/AssetManager，避免仅为图标加载全部武器蓝图及其Mesh硬依赖。此次未重构现有目录。

`SetTerminalWeaponPage(true) → HUD::PrepareWeaponIcons → AssetManager.StreamableManager.RequestAsyncLoad → OnWeaponIconsLoaded → DrawWeaponIcon`。

1. 打开/重新进入武器页时读取四个CDO的Icon路径。已驻留纹理立即缓存，其余非空路径去重后批量异步请求；HUD绘制阶段禁止同步加载。可在重新进入页面后刷新修改过的配置或重试失败资源。
2. 完成委托使用 `CreateUObject` 弱绑定HUD，无捕获Lambda；回调在游戏线程读取请求路径、把成功纹理放入 `UPROPERTY(Transient)` 强引用数组，再释放Handle。关闭菜单保留当前HUD缓存，再开无需重新读取已驻留贴图。
3. 新请求先CancelHandle再替换路径；EndPlay也取消排队委托并释放缓存。旧World、被销毁HUD或旧请求不会写入新界面。
4. 空路径合法，缺失/错误类型资源记录Warning，流送管理器也可能报告缺包；卡片继续用轮廓，装备和解锁不受影响。异步完成但GPU资源未就绪的帧同样回退。不会在每帧重试磁盘加载。
5. 图片在160×76设计像素区域内等比居中（contain），不裁切、不拉伸；已解锁保留原图颜色/Alpha。锁定状态使用UE5.4 Canvas的RGBA+Desaturation通道混合，保留透明区域并将RGB转灰、降低亮度，不需要额外材质或生成灰色图片。

建议导入带透明背景的PNG作为Texture2D，Texture Group设UI、Compression设UserInterface2D、开启sRGB，按图标用途选择NoMipmaps/NeverStream。代码不修改共享纹理的导入设置；大图应在美术资源端缩小，避免四张图标占用过多内存。

Icon是美术配置，不进入玩家存档或上传JSON。应把图标赋值并保存到实际参与Cook的武器蓝图；使用Reference Viewer核对软依赖。现有`/Game/Weapons`显式Cook规则保留。运行时才拼接的新路径需另行配置AssetManager或PrimaryAssetLabel，不能靠编辑器中能找到资源就认定打包后一定可用。本次未验证Shipping图标Cook。

图标扩展影响文件：`Weapons/DemoWeaponConfig.h`、`UI/DemoHUD.*`、`Player/DemoPlayerController.cpp`、`Tests/DemoArmoryTest.cpp`及本说明；无需增加运行时模块依赖。新增入口/异步回调使用调用日志，绘制/缓存读取使用高频日志。

验证图标时在上述Armory真实渲染命令追加 `-DemoWeaponIconTest`。仅测试进程临时把手枪/步枪CDO指向同一彩色样例纹理，散弹留空，狙击使用刻意不存在的测试路径，检查真实软引用完成、去重共享对象、空/缺失回退及彩色/灰色截图；不保存蓝图，不把样例纹理作为正式武器美术。此用例出现测试缺包日志属于预期，仍需检查最终 `DEMO_ARMORY_SUCCESS`。

2026-09-20 图标扩展验证：Editor Development编译通过（`Saved/Logs/WeaponIconBuild.log`）；1280×720真实渲染运行 `-DemoArmoryTest -DemoWeaponIconTest`，三个难度均通过图标加载/共享及空/失效路径回退断言，最终输出 `DEMO_ARMORY_SUCCESS`，进程退出码0（`Saved/Logs/WeaponIconValidation.log`）。已检查锁定目录截图，彩色样例与置灰样例、轮廓回退均显示正常，截图独立归档到 `Art/UI/WeaponIconValidation/`；样例是测试砖墙纹理，正式蓝图未赋值该纹理。此次未重测4:3或Shipping打包。

## 验证方法、影响文件与维护

新专项使用 `-DemoArmoryTest`，必须真实渲染（不能-nullrhi），每World90秒超时。依次完整清空简单/普通/困难十关，检验只解锁对应武器、不自动装备、重复胜利幂等、每次重开只有手枪、卡片真实缩放热区、锁定Tips、模态阻止穿透、终端零金币装备、射击库存切换不补弹、距离拒绝、B不可绕过、读回本地数据、版本保护/备份恢复、上传DTO/错档旧回复拒绝及死亡保留永久进度。测试通过GE击杀怪物验证业务流程，不代表人工射击手感或平衡性验收。

`-DemoArmoryTest`、`-DemoWeaponTest`、`-DemoUIValidation`、`-DemoSmokeTest`、`-DemoCampaignTest` 自动创建独立GUID测试槽并在GI退出删除，只清理本实例创建的槽。不得同时运行多个测试状态机参数。旧武器专项已迁移为终端装备夹具，不能恢复初始步枪或B换枪以迁就旧断言。

```powershell
& 'F:/UE5.4.4/UnrealEngine-5.4.4-release/Engine/Binaries/Win64/UnrealEditor-Cmd.exe' 'F:/UE5.4Project/FPSDemo/FPSDemo/FPSDemo.uproject' /Game/Whitebox/Maps/L_ThreeSector_Whitebox -game -DemoArmoryTest -RenderOffscreen -windowed -ForceRes -ResX=1280 -ResY=720 '-ExecCmds=r.SetRes 1280x720w' -unattended -nosound '-LogCmds=LogFPSDemo Log'
```

成功标记 `DEMO_ARMORY_SUCCESS`，失败 `ARMORY_TEST FAIL`；截图在 `Saved/Screenshots/Armory`。重点日志：`PROFILE_LOAD/SAVE`、`PROFILE victory`、`TERMINAL_PAGE`、`TERMINAL_EQUIPPED`。所有新增成员函数均使用项目日志宏；无UObject的目录函数使用同级VeryVerbose入口日志。

影响文件：`Player/DemoPlayerProfile.*`、`Weapons/DemoWeaponCatalog.*`、`Weapons/DemoWeaponComponent.*`、`Player/DemoPlayerController.*`、`Game/FPSDemoGameMode.*`、`UI/DemoHUD.*`、`FPSDemo.Build.cs`、`Tests/DemoArmoryTest.*`、已有武器/UI/Smoke测试与相应模块文档。

编译、专项回归与截图的本次实际结果见下方后续记录；下列记录为原武器库版本验收；新增云同步验证单独记录在《云存档运行与验证.md》。Shipping打包和竞技防作弊尚未验证。

2026-09-20 武器库版本验证记录：

- Editor Development最终整合构建通过（`Saved/Logs/ArmoryIntegrationBuild.log`）。包含暂停/设置/三槽战役存档相关代码的编译，但本模块测试不替代这些页面各自的完整业务验收。
- `Saved/Logs/ArmoryValidation.log` 与 `ArmoryValidation4x3.log` 均输出 `DEMO_ARMORY_SUCCESS`；包含三难度完整十关、真实终端、存档重新加载、坏主档备份恢复、未来版本保护和上传确认边界。
- 已检查1280×720、1024×768实际武器库/Tips截图，分别归档 `Art/UI/Armory/1280x720/` 与 `1024x768/`；灰色锁定卡、真实基础数值、解锁条件、装备成功反馈可见。
- `ArmoryWeaponRegression.log` 输出 `DEMO_WEAPON_SUCCESS`，旧武器输入/装填/瞄准专项通过终端夹具适配。
- `ArmoryUIRegression.log` 输出 `DEMO_UI_SUCCESS`，覆盖当时大厅、商店/奖励、胜败与死亡重开UI。
- Smoke旧断言已迁移为默认手枪20伤害（60HP目标剩40）；最终 `ArmorySmokeRegression.log` 输出 `DEMO_SMOKE_SUCCESS`，十关、GAS、商店、死亡/跌落及重开通过。验证以日志成功标记为准，不只看进程退出码。
- 最终整合回归 `ArmoryIntegratedValidation.log` 输出 `DEMO_ARMORY_SUCCESS`，追加验证暂停覆盖时Controller与组件均拒绝装备，关闭暂停恢复原Tips；更新后的 `ArmoryWeaponRegression.log` 再次输出 `DEMO_WEAPON_SUCCESS`。最终1280×720武器页截图归档覆盖到同一 `Art/UI/Armory/1280x720/` 目录。

并行扩展注意：永久玩家档案与新增 `Save/DemoRunSave.*` 战役检查点是不同数据域。永久解锁独立于战役槽，金币永久成长在同槽重开保留，银币临时成长重置；载入同一未结束战役可恢复之前在终端装备过的库存，这不等于新建战役自动发放已解锁武器。终端操作新增 `HasBlockingOverlay()` 二次保护，暂停/设置页覆盖时拒绝底层装备、切页、关闭Tips和保存请求。

专项隔离边界：大厅新增存档选择后，Armory/Weapon/Smoke测试直接通过GameMode建立无活动战役槽的新局，再调用OnRunReady恢复输入；Armory通过初始Hub的权威接口指定三难度。存档选择页本身由Session专项负责。这些测试仍使用真实终端、完整战役清关、真实永久存档和生产装备验证，不向正式档案注入解锁。
